{
  "name": "watch-design-studio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/relief3d.js --bundle --format=iife --minify --outfile=vendor/relief3d.bundle.js",
    "test": "node --test tests/*.test.cjs tests/*.test.js"
  },
  "devDependencies": { "esbuild": "0.25.10", "three": "0.180.0" }
}
